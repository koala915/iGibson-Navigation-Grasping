# 交接：2026-09-20（分支 `v23-grasp-test`）

**一句話狀態**：機器上的 v23 已同步成 repo 版（2026-09-25），夾取約 **7.2 秒**、放下約
**4.6 秒**；常駐服務重啟 11.6 秒就緒、記憶體約 487 MB；夾取 ↔ 導航切換與握物放下都已實機驗證。
剩下的主要是**需要拿尺的驗證**（LEFT／RIGHT 映射、E1 視野與搆及範圍）和 **G2**。

> **G2 分工（2026-09-25 決定）：由 koala915 負責**，把 v23 接進 `integration/mission_pipeline.py`，讓導航與夾取共用同一個序列埠連線、切換時不再重啟服務（目前每次切換 11.6 秒）。導航端（WP2：odom、LiDAR、AMCL）仍由 `112303577ncu` 負責。計畫見 [`GRASP_LATENCY_PLAN_2026-09-21.md`](../planning/GRASP_LATENCY_PLAN_2026-09-21.md)。

> **2026-09-22 導航續報**：WP2 已完成底盤直線點動與第一輪尺量校正。正常馬達輸出 30
> 下，舊 linear scale `0.65` 的 odom 為 14.6 cm、尺量約 22 cm；改為 `0.98` 後 odom
> 20.9 cm、尺量約 21--22 cm。`0.98` 已同步為 repo 預設，但仍須較長直線重複測試；
> angular `0.501`、LiDAR mainline 方向旗標、TF／AMCL 與自主巡航都尚未驗收。完整證據見
> [`progress.md`](../../progress.md) 的 2026-09-22 段落。Jetson 部署用
> `ai_motor_server_P0.py` 仍不在 repo，只有機器上的檔案已套用該值並保留 `0.65` 備份。

這份是接手的起點。規格不在這裡，在 [`CLAUDE.md`](../../CLAUDE.md)。

---

## 0. 給 AI 助理的閱讀順序

| 想知道什麼 | 讀哪一份 |
|-----------|---------|
| 完整規格、部署指令、關節映射、三段式控制 | [`CLAUDE.md`](../../CLAUDE.md) ← **先讀這份** |
| 要改的程式在哪一支 | [`INDEX.md`](../../INDEX.md) |
| 這個坑有沒有人踩過 | [`TROUBLESHOOTING.md`](../../TROUBLESHOOTING.md) |
| 每天發生了什麼、實機數據 | [`progress.md`](../../progress.md)（新到舊，先看 2026-09-20） |
| v23 這條線的細節與合併條件 | [`grasp/v23/README.md`](../../grasp/v23/README.md) |

**這個專案的文件有一個必須延續的慣例：區分「已驗證」與「跑過一次」。**
manifest 裡的 `hardware_validated` 之類的旗標只有在有尺量數據時才會變 true。
不要把單次成功寫成驗證通過，也不要為了讓流程跑起來而拿掉 gate —— 那些 gate 幾乎
每一道都是某次實機事故留下來的。

---

## 1. 現在能跑什麼

### 常駐夾取服務（本次新增，定點夾取用）

```bash
python3 graspctl.py status     # 服務狀態、伺服角度、目前偵測是否新鮮
python3 graspctl.py grasp      # 夾一次，約 8.5 秒
python3 graspctl.py release    # 伸手、開爪、回 home（丟垃圾桶那一段）
python3 graspctl.py home       # 原地張爪回 E1，把物體放回桌上
```

在 `grasp/v23/` 底下執行，用系統 `python3`（3.6.9）即可，不必進 venv。
服務本體由 systemd 開機帶起（`grasp-service` + `grasp-vision`），開機後約 30 秒可用。

原理：`grasp_service.py` 呼叫 launcher 自己的 `build_ctrl_cmd()` 取得**完全相同的
參數**，再把 `GraspController.run` 換成等指令的迴圈。所有安全閘照跑，只是從「每次跑」
變成「啟動時跑一次」。42.3 秒裡有 34 秒是重複載入 torch／PyBullet／PPO／YOLO。

⚠ **服務活著時獨占 `/dev/myserial` 和相機。** 要用 `move_arm.py`、`bus_probe.py`、
`jetson_one_command_grasp.py` 之前一定要先：

```bash
sudo systemctl stop grasp-vision grasp-service
```

忘了停的症狀是「裝置打不開」，看起來很像硬體壞掉。

### 三姿態掃描（後備，物體不在 E1 視野時）

```bash
./graspscan.sh --accept-single-rotated-view
```

會自己停服務 → 跑 LEFT/E1/RIGHT 掃描與夾取 → 再把服務拉回來（`trap EXIT`）。
走這條要付完整的 23 秒啟動成本，所以只在 E1 看不到物體時用。

---

## 2. 已驗證 vs 只跑過一次

| 項目 | 狀態 |
|------|------|
| 定點 E1 夾取 8.5 秒 | **可重複**：連續 6 輪、成功 3 次分別 8.05／8.34／8.53 秒 |
| 常駐服務不漏記憶體 | **已驗**：三輪 RSS 都是 986.1 MB，完全走平 |
| 空爪不會假裝放下 | **已驗**：夾爪**完全張開**時 0.04 秒被拒、手臂不動 |
| 握著物體的正向放下 | **已驗（2026-09-25）**：6 步伸手全到位，指墊離地 11.6 cm 開爪，盒子落在 x≈0.283 |
| 搬運途中掉落 | **偵測不到，已決定不處理（2026-09-25）**：夾爪只多閉 2° 仍判定握著；夾持力夠大，接受此風險 |
| 夾取 ↔ 導航模式切換 | **已驗（2026-09-25）**：先停完才啟動、全程一個序列埠持有者 |
| 精簡 URDF（deploy） | **已上機並實機確認（2026-09-25）**：幾何 0 差異；冷啟動 23.1 → 11.5 秒（暖）、記憶體 985 → 486 MB；夾取＋放下行為不變 |
| repo 版 v23 在 Jetson 自測 | **已驗（2026-09-25）**：163／37／641／62／89、dry-run、安全閘全過 |
| 時序 A/B | **已驗（2026-09-25）**：新時序夾取 −1.3 s、放下 −3.4 s，接觸判定無退化（每組 2–3 樣本） |
| 機器同步成 repo 版 v23 | **已完成（2026-09-25）**：8 個檔、備份在 `~/sync_backup_20260925.tar.gz`；實機夾取 7.21 s、放下 4.58 s |
| LEFT 姿態真機夾取 | **只跑過一次**（單視角）。沒有尺量點，`hardware_validated` 仍是 false |
| RIGHT 姿態 | **完全沒走過** |

---

## 3. 下一步，照順序

**(1) v23 已同步成 repo 版（2026-09-25），留意還沒被驗到的部分。** 懸停後備改成 15 步
從沒觸發過；`--jaw-step-deg`、`--wait-for-go`（一鍵啟動器用）沒上過機；時序 A/B 每組只有
2–3 個樣本。另外今天看到一次**放下時夾爪張到一半卡住**，控制器正確地凍結不收回，但原因
（物體卡住夾爪或機構問題）還不確定，值得在之後的測試裡留意。

**(2) 補 LEFT／RIGHT 的尺量驗證，把 `--unlock-unvalidated-scan` 的存在理由消掉。**
流程寫在 `grasp/v23/README.md`：每個姿態至少放 2 個分散的尺量點，比對 `predicted_x/y`
與尺量 base 座標，每軸誤差 ≤1 cm，才把 `three_pose_scan.json` 的兩個旗標改成 true。

---

## 4. ⚠ 機器上的檔案跟這個 repo 只有部分同步

Jetson 上 `~/Documents/deploy_jetson2/` 不是 git repo。2026-09-25 起：

- **已同步**：`grasp/v23/`（逐位元組與 repo 相同）與 `integration/vision_grasp_bridge.py`；
  `grasp/x3plus/` 多了 `make_deploy_urdf.py` 與 `yahboomcar_deploy.urdf`（與 repo 相同）；
  `deploy/systemd/` 的單元與腳本也已裝到 `/etc/systemd/system/` 與 `/usr/local/libexec/x3plus/`。
- **仍是 2026-08-30 快照**：`grasp/v21/`（另外打了空爪檢查與精簡 URDF 兩個補丁，備份為
  `*.bak_graspgate_*`、`*.bak_deployurdf_*`）以及 `integration/` 的其他檔案。
- **只存在機器上、不在 repo**：`integration/` 裡的 `calibrate_feedback_odom_linear.py`、
  `calibrate_feedback_odom_yaw.py`、`remote_vision_source.py`、`sugarbox_rl_motor_server.py`。
  這些沒有版本控制，換碟或重灌就會消失。

更新時照今天的做法：先用 `diff --strip-trailing-cr` 找出真正不同的檔、確認有誰依賴它、
整份備份，再換，每換一批都重跑自測並實機夾一次。

---

## 5. 環境與存取

- Jetson 使用者固定是 `jetson`，**IP 每次開機都會變**，要當場查（`hostname -I`）。
- 完整環境在 `~/grasp_venv`（Python 3.8）。系統 `python3` 是 3.6.9，跑不動 torch。
- 系統碟 2026-09-20 換成 128 GB（根目錄在 `/dev/sda1`），開機檢查全過，細節見
  `progress.md`。torch 是 **CPU-only**（JetPack 4.6 沒有對應的 CUDA wheel），
  YOLO 推論在 CPU 上約 0.25 秒／張（`--imgsz 320`）。
- ROS 那一套的啟動指令與各自的記憶體佔用，記在 `progress.md` 的 2026-09-20 段落。
  全套約 569 MB，跟常駐服務（約 1.3 GB）可以並存，還剩約 1 GB。

---

## 6. 今天的 commit

```
952a255 WIP: everything on this branch that had not been committed yet
99bc73d Write up the rest of 2026-09-20
fa66c22 An open jaw is not a grasp
393f0fa Give the service a way to put the object back down
890ff7a Record 2026-09-20: the new disk, the timing budget, and LEFT's first grasp
2e36ddb Pay the grasp pipeline's startup cost once instead of every grasp
```

`952a255` 是接手前累積的未提交工作，**一次推上來的快照，不是完成品**；訊息裡逐項
寫了它包含什麼、哪些跑過測試、哪些沒上過機。
